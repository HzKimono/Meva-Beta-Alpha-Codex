# Replay Dataset

Required folders: candles/, orderbook/. Optional folder: ticker/.
One CSV per symbol (e.g., BTCTRY.csv).
Run `python -m btcbot.cli replay-init --dataset .\data\replay --seed 123` for synthetic data.
