version: '3.9'
services:
  btcbot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env.live
    volumes:
      - btcbot-data:/data
    command: ["run", "--loop", "--cycle-seconds", "10"]

volumes:
  btcbot-data:
